# Flutter Color Picker (scraped)

(Extracted from https://pub.dev/packages/flutter_colorpicker)

- Use inside `showDialog` with `ColorPicker`, `BlockPicker` or `MaterialPicker`.
- Example BlockPicker usage:

```dart
BlockPicker(
  pickerColor: currentColor,
  onColorChanged: changeColor,
),
```

(Full extracted content saved.)