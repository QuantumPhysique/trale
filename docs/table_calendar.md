# Table Calendar (scraped)

(Extracted from https://pub.dev/packages/table_calendar)

- Install: `table_calendar: ^3.2.0` (example)
- Use `eventLoader` to provide events per day, typically backed by `Map<DateTime, List<T>>`.
- Customize `calendarStyle` and `headerStyle` for visual appearance and marker decoration.

(Full extracted content saved.)