import 'package:quantumphysique/quantumphysique.dart';

/// Trale alias for [QPSliverAppBarSnap].
///
/// All pages use this typedef so import paths remain stable while the
/// implementation lives in the shared QP package.
typedef SliverAppBarSnap = QPSliverAppBarSnap;
